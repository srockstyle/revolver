name: "API: CI"

on:
  push:
    branches:
      - main
    paths:
      - "app/controllers/api/**"
      - "spec/requests/api/**"
  workflow_dispatch:
    inputs:
      environment:
        description: Development Environment
        required: true
        type: environment

jobs:
  ci:
    name: app ci
    uses: ./.github/workflows/openapi-generater.yaml
    with:
      app_name: api
